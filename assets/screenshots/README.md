Place real plugin screenshots here. WordPress.org expects files named `screenshot-1.png`, `screenshot-2.png`, etc.

To replace the placeholders, add PNG images with these exact filenames to this folder.

This folder currently contains simple SVG placeholders you can preview in a browser and replace with real screenshots.